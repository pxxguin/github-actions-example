name: Conditions
on: push
jobs:
    skip-or-run:
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo "Hello, World!"
                echo "${{github.run_id}}"
              if: ${{ contains(github.run_attempt, '1')}}