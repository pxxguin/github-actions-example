name: Conditions
on: workflow_dispatch
jobs:
    skip-or-run:
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo "Hello, World!"
                echo "${{github.run_id}}"
              if: ${{ contains(github.run_id, '1')}}