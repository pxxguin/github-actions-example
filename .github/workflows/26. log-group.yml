name: Log grouping
on: workflow_dispatch
jobs:
    logs:
        runs-on: ubuntu-latest
        steps:
            - run:
                echo "::group::Show environment variables"
                printenv
                echo "::endgroup::"