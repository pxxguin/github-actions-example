name: Dependency jobs code
on: workflow_dispatch
jobs:
    test-1:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This is fucking first steps."
    test-2:
        runs-on: ubuntu-latest
        needs: [test-1]
        steps:
            - run: echo "This is fucking second steps."
    test-3:
        runs-on: ubuntu-latest
        needs: [test-2]
        steps:
            - run: echo "HaHa, This is final steps!!"