name: Add mask
on: workflow_dispatch
jobs:
    logs:
        runs-on: ubuntu-latest
        env:
            PASSWORD: Supersonic
        steps:
            - run: echo "::add-mask::${PASSWORD}"
            - run: echo "${PASSWORD}"